
#pragma once

const Deck simple_deck =
{
	{
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&Forest::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data,
		&ChargingBadger::cardclass_data
	}
};
